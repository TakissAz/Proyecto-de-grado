import type { HistorialPlanes } from '@/Components/nutricionista/planes/HistorialPlanesNutricionista';
const e=(v:string)=>v.replaceAll('_',' ');const n=(v:number)=>Number(v??0).toLocaleString('es-BO',{maximumFractionDigits:1});
export default function HistorialPlanesPaciente({historial}:{historial:HistorialPlanes|null}){if(!historial)return null;return <section className="space-y-3"><div><h2 className="text-lg font-bold">Historial de mis planes</h2><p className="text-sm text-base-content/60">Consulta tus planes validados y los cambios realizados por nutrición.</p></div><div className="rounded-2xl border border-primary/20 bg-primary/5 p-4"><b>Cambios respecto al plan anterior</b><p className="mt-1 text-sm">{historial.comparacion.mensaje}</p></div>{historial.planes.length===0?<div className="rounded-2xl border border-dashed border-base-300 p-6 text-center text-sm">Aún no tienes planes en el historial.</div>:<div className="grid gap-3 lg:grid-cols-2">{historial.planes.map(p=><details className="rounded-2xl border border-base-300 bg-base-100 p-4" key={p.id_plan_alimentario}><summary className="cursor-pointer list-none"><div className="flex justify-between gap-2"><div><b>{p.nombre_plan}</b><p className="text-xs text-base-content/50">{p.fecha_inicio??'—'} al {p.fecha_fin??'—'}</p></div><span className="badge badge-success capitalize">{e(p.estado_plan)}</span></div><p className="mt-2 text-sm">{p.resumen}</p><div className="mt-2 flex gap-2 text-xs"><span>{n(p.calorias_planificadas)} kcal</span><span>· {p.total_comidas} comidas</span><span>· {n(p.porcentaje_adherencia)}%</span></div></summary><div className="mt-4 space-y-2 border-t border-base-300 pt-3">{p.dias.map(d=><div key={d.numero_dia}><b className="text-sm">{d.nombre_dia}</b><ul className="ml-4 list-disc text-xs">{d.comidas.map((c,i)=><li key={i}><span className="capitalize">{e(c.tipo_comida)}</span>: {c.nombre_comida}</li>)}</ul></div>)}</div></details>)}</div>}</section>}

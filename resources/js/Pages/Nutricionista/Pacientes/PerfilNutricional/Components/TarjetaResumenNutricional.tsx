import { Activity, CheckCircle2, Ruler, Scale } from 'lucide-react'; import type { Registro } from '../tipos';
export default function TarjetaResumenNutricional({evaluacion,completadas}:{evaluacion:Registro|null;completadas:number}) { const items=[['Peso',evaluacion?.peso?`${evaluacion.peso} kg`:'—',Scale],['IMC',evaluacion?.imc??'—',Activity],['ICC',evaluacion?.indice_cintura_cadera??'—',Ruler],['Secciones',`${completadas}/6`,CheckCircle2]] as const; return <section className="grid grid-cols-2 gap-3 lg:grid-cols-4">{items.map(([label,value,Icon])=><div className="card border border-base-300 bg-base-100 shadow-sm" key={label}><div className="card-body flex-row items-center gap-3 p-4"><span className="grid size-10 place-items-center rounded-xl bg-emerald-500/10 text-emerald-600"><Icon size={19}/></span><div><p className="text-xs text-base-content/55">{label}</p><p className="text-lg font-bold">{String(value)}</p></div></div></div>)}</section>; }

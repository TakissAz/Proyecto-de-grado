import FormularioBase from './FormularioBase'; import type { Registro } from '../tipos';
export default function FormularioRestriccionesAlimentarias(p:{abierto:boolean;cerrar:()=>void;registro:Registro|null;pacienteId:number}) { const id=p.registro?.id_restriccion_alimentaria; return <FormularioBase abierto={p.abierto} cerrar={p.cerrar} titulo={id?'Editar restricciones':'Registrar restricciones'} registro={p.registro} url={`/nutricionista/pacientes/${p.pacienteId}/perfil-nutricional/restricciones${id?`/${id}`:''}`} campos={['alergias','intolerancias','alimentos_restringidos','alimentos_no_tolerados','alimentos_rechazados','observaciones'].map((name)=>({name,label:name.replaceAll('_',' '),type:'textarea'}))}/>; }

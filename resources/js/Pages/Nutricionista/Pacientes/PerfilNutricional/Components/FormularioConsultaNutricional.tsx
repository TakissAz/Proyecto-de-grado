import FormularioBase from './FormularioBase'; import type { Opciones, Registro } from '../tipos';
export default function FormularioConsultaNutricional(p:{abierto:boolean;cerrar:()=>void;registro:Registro|null;pacienteId:number;opciones:Opciones}) { const id=p.registro?.id_consulta_nutricional; return <FormularioBase abierto={p.abierto} cerrar={p.cerrar} titulo={id?'Editar consulta':'Registrar consulta'} registro={p.registro} url={`/nutricionista/pacientes/${p.pacienteId}/perfil-nutricional/consulta${id?`/${id}`:''}`} campos={[{name:'fecha_consulta',label:'Fecha de consulta',type:'date'},{name:'estado_consulta',label:'Estado',type:'select',options:p.opciones.estado_consulta},{name:'motivo_consulta',label:'Motivo',type:'textarea'},{name:'observaciones_generales',label:'Observaciones generales',type:'textarea'}]}/>; }

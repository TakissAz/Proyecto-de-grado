import FormularioBase from './FormularioBase'; import type { Registro } from '../tipos';
export default function FormularioPreferenciasAlimentarias(p:{abierto:boolean;cerrar:()=>void;registro:Registro|null;pacienteId:number}) { const id=p.registro?.id_preferencia_alimentaria; return <FormularioBase abierto={p.abierto} cerrar={p.cerrar} titulo={id?'Editar preferencias':'Registrar preferencias'} registro={p.registro} url={`/nutricionista/pacientes/${p.pacienteId}/perfil-nutricional/preferencias${id?`/${id}`:''}`} campos={['alimentos_preferidos','alimentos_no_preferidos','comidas_preferidas','comidas_frecuentes','preparaciones_preferidas','sabores_preferidos','observaciones'].map((name)=>({name,label:name.replaceAll('_',' '),type:'textarea'}))}/>; }

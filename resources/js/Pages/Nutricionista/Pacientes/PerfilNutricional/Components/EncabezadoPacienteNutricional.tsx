import { Link } from '@inertiajs/react'; import { ArrowLeft, CalendarDays, IdCard, Salad } from 'lucide-react'; import type { PacienteNutricional } from '../tipos';
export default function EncabezadoPacienteNutricional({paciente}:{paciente:PacienteNutricional}) { const nombre=[paciente.nombres,paciente.apellido_paterno,paciente.apellido_materno].filter(Boolean).join(' '); return <header className="overflow-hidden rounded-3xl bg-gradient-to-br from-emerald-700 via-teal-700 to-cyan-800 p-6 text-white shadow-lg sm:p-8"><div className="flex flex-col justify-between gap-6 sm:flex-row sm:items-end"><div><div className="mb-5 flex size-12 items-center justify-center rounded-2xl bg-white/15 backdrop-blur"><Salad size={25}/></div><p className="text-xs font-semibold uppercase tracking-[0.22em] text-emerald-100">Perfil nutricional</p><h1 className="mt-2 text-2xl font-bold sm:text-3xl">{nombre}</h1><div className="mt-3 flex flex-wrap gap-4 text-sm text-white/80"><span className="flex items-center gap-1.5"><IdCard size={15}/> CI {paciente.ci}</span><span className="flex items-center gap-1.5"><CalendarDays size={15}/> {paciente.fecha_nacimiento}</span></div></div><Link href={`/nutricionista/pacientes/${paciente.id_paciente}`} className="btn border-white/20 bg-white/10 text-white hover:bg-white/20"><ArrowLeft size={16}/> Volver al paciente</Link></div></header>; }

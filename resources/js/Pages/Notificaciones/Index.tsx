import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';
import { Head, router } from '@inertiajs/react';
import { Bell, CheckCheck } from 'lucide-react';
type N={id_notificacion_interna:number;titulo:string;mensaje:string;prioridad:string;leida:boolean;url_destino:string|null;created_at:string};
export default function Index({listadoNotificaciones}:{listadoNotificaciones:{data:N[]}}){return <AuthenticatedLayout title="Notificaciones"><Head title="Notificaciones"/><main className="mx-auto max-w-4xl space-y-4"><div className="flex justify-end"><button onClick={()=>router.post('/notificaciones/leer-todas')} className="flex items-center gap-2 rounded-xl border px-4 py-2 text-xs"><CheckCheck size={15}/>Marcar todas como leídas</button></div>{listadoNotificaciones.data.length?listadoNotificaciones.data.map(n=><button key={n.id_notificacion_interna} onClick={()=>router.post(`/notificaciones/${n.id_notificacion_interna}/leer`)} className="card-elevated flex w-full gap-3 p-4 text-left"><Bell size={18} className={n.leida?'text-ink-muted':'text-brand-green'}/><div><p className="text-xs font-bold">{n.titulo}</p><p className="mt-1 text-[11px] text-ink-muted">{n.mensaje}</p><p className="mt-2 text-[9px] uppercase text-ink-muted">Prioridad {n.prioridad}</p></div></button>):<div className="card-elevated p-10 text-center text-sm text-ink-muted">Sin notificaciones.</div>}</main></AuthenticatedLayout>}

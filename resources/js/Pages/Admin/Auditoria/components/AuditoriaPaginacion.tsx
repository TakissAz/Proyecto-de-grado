import clsx from 'clsx';
import { ChevronLeft, ChevronRight } from 'lucide-react';
export default function AuditoriaPaginacion({mostrando,total,pagina,ultima,onCambiar}:{mostrando:number;total:number;pagina:number;ultima:number;onCambiar:(p:number)=>void}) { return <div className="flex flex-col items-center justify-between gap-3 border-t border-surface-border px-5 py-3.5 dark:border-surface-border-dark sm:flex-row"><p className="text-[11.5px] text-ink-muted dark:text-ink-muted-dark">Mostrando {mostrando} de {total} pacientes</p>{ultima>1&&<div className="flex gap-1"><button disabled={pagina===1} onClick={()=>onCambiar(pagina-1)} className="flex h-7 w-7 items-center justify-center rounded-lg text-ink-muted disabled:opacity-30"><ChevronLeft size={13}/></button>{Array.from({length:ultima},(_,i)=>i+1).map(p=><button key={p} onClick={()=>onCambiar(p)} className={clsx('h-7 w-7 rounded-lg text-[11px] font-semibold',p===pagina?'bg-brand-green text-white':'text-ink-muted hover:bg-[#F5F3EE] dark:text-ink-muted-dark dark:hover:bg-white/[0.04]')}>{p}</button>)}<button disabled={pagina===ultima} onClick={()=>onCambiar(pagina+1)} className="flex h-7 w-7 items-center justify-center rounded-lg text-ink-muted disabled:opacity-30"><ChevronRight size={13}/></button></div>}</div>; }
